'use strict';

/**
 * whitespace(elem) removes extraneous whitespace from an element.
 * Returns the given element.
 *
 * @param {Element} elem
 * @return {Element}
 */
function whitespace (elem) {

}

module.exports = whitespace
